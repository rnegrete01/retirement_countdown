==================================
Chapter 8 - Date(Plus Time) Object
==================================
Clock
	get the current date using new Date()
	getHours getMinutes getSeconds
	setInterval
	
Countdown
	create a date using new Date(string)
	how to test for invalid using toString == Invalid Date
	how to do math with a date object
	template literal
	
slide_show
	html: <ul><li>... CSS ul { display: none; }
	querySelectorAll use to create imageCache using a for loop
	setInterval to update src & alt properties
	template literal
	
timer
	how to always have a leading 0 for hours & minutes
		const padValue = value => value.toString().padStart(2, "0");
	parseInt prompt
	endTime.setMinutes(endTime.getMinutes + minutes)
	timer = setInterval
		diff = entTime.getTime() - now.getTime()
		if diff < 0
			clearInterval(timer)
		else
			new Date(diff)
			padValue Minutes & Seconds
			template literal
			
trivia
	input radio
	timer = setInterval(updateTime)
	querySelectorAll("input") // radio
	for(let opt of options)
		addEventListener
	updateTime	
		seconds = parseInt(span.textContent) - 1;
		if seconds == 0 clearInterval(timer)
	processAnswer
		clearInterval(timer)
		
======================================
Chapter 9 Data Validation & Exceptions
======================================
register (NO JavaScript - Just HTML data validation)
	input type=email attributes[placeholder, autocomplete, autofocus, required]
	input type=date (NOTE: v2 added title attribute)	
	input type=tel attributes[placeholder, autocomplete, pattern title]
	select attribute[required] (NOTE: v2 added title attribute)
	input type=radio attribute[checked] (NOTE: v2 added title attribute)
	input type=checkbox attribute[required]
	
register_2.0 (Disables HTML data validation to use customized JavaScript data validation instead)
	clearMessage
		for of form.elements 
			nextElementSibling set all spans textContent = "*"
			
	form.noValidate = true
	for of form.elements addEventListener("invalid")
		msg = title attribute OR validationMessage property
		NOTE: title = set manually in html and shows on hover
			  validationMessage = generated by the browser (but can be overridden)
			    returns a localized message 
				describing the validation constraints 
				that the control doesn't satisfy
				
	add submit listener
		msg = (dob > limit) ? dobElement.title: "";
        dobElement.setCustomValidity(msg);
		if !form.checkValidity
			preventDefault
			
	add reset_from listener
		clearMessage
		email_address.focus()
		
future_value (adding try-catch & throw)
	
	calc_future_value.js
        throw new Error("Years must be a number greater than zero.");

	fucture_value.js
		try
			calcFutureValue (built in JavaScript function)
        catch(e
			const msg = `${e.name}: ${e.message}`;
			getElement("#message").textContent = msg;
	
	
